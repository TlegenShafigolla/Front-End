import makeStyles from "@material-ui/core/styles/makeStyles";

export const useStyle = makeStyles(theme => ({
    drawer: {
        width: 240,
        flexShrink: 0,
    },
    drawerPaper: {
        width: 240,
        background:'#272827'
    },
}))

