
let initialState = {
    email: '',
    password: '',
    loggedIn: false,
    disabledButton: false,
    error: false
}
const LoginReducer = (state = initialState, action) => {
    switch (action.type) {

        default :
            return state
    }

}
export default LoginReducer;